---
# CRD gerado pelo debezium-operator 3.5.1 (ausente no helm template sem --include-crds)
# Schema x-kubernetes-preserve-unknown-fields aceita todos os campos definidos pela API
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  name: debeziumservers.debezium.io
  labels:
    app.kubernetes.io/name: debezium-operator
    app.kubernetes.io/version: 3.5.1
spec:
  group: debezium.io
  names:
    kind: DebeziumServer
    listKind: DebeziumServerList
    plural: debeziumservers
    singular: debeziumserver
    shortNames:
      - dbs
  scope: Namespaced
  versions:
    - name: v1alpha1
      served: true
      storage: true
      subresources:
        status: {}
      additionalPrinterColumns:
        - jsonPath: .status.conditions[?(@.type=="Ready")].status
          name: Ready
          type: string
        - jsonPath: .status.conditions[?(@.type=="Ready")].message
          name: Reason
          type: string
        - jsonPath: .metadata.creationTimestamp
          name: Age
          type: date
      schema:
        openAPIV3Schema:
          type: object
          x-kubernetes-preserve-unknown-fields: true
          properties:
            spec:
              type: object
              x-kubernetes-preserve-unknown-fields: true
            status:
              type: object
              x-kubernetes-preserve-unknown-fields: true
