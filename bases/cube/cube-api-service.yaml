apiVersion: v1
kind: Service
metadata:
  name: cube-api
  labels:
    app: cube-api
spec:
  type: ClusterIP
  selector:
    app: cube-api
  ports:
    - name: http
      port: 4000
      targetPort: 4000
      protocol: TCP
