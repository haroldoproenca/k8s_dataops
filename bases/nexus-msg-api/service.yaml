apiVersion: v1
kind: Service
metadata:
  name: nexus-msg-api-service
  labels:
    app: nexus-msg-api
spec:
  type: ClusterIP
  selector:
    app: nexus-msg-api
  ports:
  - name: http
    protocol: TCP
    port: 80
    targetPort: 8000

