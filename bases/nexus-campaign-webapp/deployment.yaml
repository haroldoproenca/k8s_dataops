apiVersion: apps/v1
kind: Deployment
metadata:
  name: nexus-campaign-webapp
  labels:
    app: nexus-campaign-webapp
spec:
  replicas: 1
  selector:
    matchLabels:
      app: nexus-campaign-webapp
  strategy:
    type: RollingUpdate
    rollingUpdate:
      maxSurge: 1
      maxUnavailable: 0
  template:
    metadata:
      labels:
        app: nexus-campaign-webapp
    spec:
      containers:
      - name: nexus-campaign-webapp
        image: ghcr.io/synapsehealthtech/nexus-campaign-webapp:latest
        imagePullPolicy: Always
        ports:
        - name: http
          containerPort: 80
          protocol: TCP
        # resources: definidos no overlay (OKE obrigatório, local omitido)
        volumeMounts:
        - name: env-js
          mountPath: /usr/share/nginx/html/env.js
          subPath: env.js
        livenessProbe:
          httpGet:
            path: /health
            port: 80
          initialDelaySeconds: 10
          periodSeconds: 15
          timeoutSeconds: 3
          failureThreshold: 3
        readinessProbe:
          httpGet:
            path: /health
            port: 80
          initialDelaySeconds: 5
          periodSeconds: 5
          timeoutSeconds: 3
          failureThreshold: 3
      volumes:
      - name: env-js
        configMap:
          name: nexus-campaign-webapp-env
