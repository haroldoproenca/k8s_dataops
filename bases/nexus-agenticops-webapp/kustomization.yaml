apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

# O namespace nexus-agenticops é declarado na base nexus-agenticops-api.
# Este kustomization não redeclara o namespace — aplique o overlay da API primeiro.
resources:
  - webapp-deployment.yaml
  - webapp-service.yaml
