apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: clickhouse-pvc
  labels:
    app: clickhouse
    component: agenticops
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 10Gi
