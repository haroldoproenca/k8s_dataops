# Credenciais do PostgreSQL externo (Ubuntu host, acessível via host.k3d.internal)
# Criar no Ubuntu antes de aplicar:
#   CREATE ROLE marquez LOGIN PASSWORD 'marquez';
#   CREATE DATABASE marquez OWNER marquez;
#   -- em pg_hba.conf adicionar: host marquez marquez 172.18.0.0/24 md5
#   -- SELECT pg_reload_conf();
apiVersion: v1
kind: Secret
metadata:
  name: marquez
  namespace: marquez
type: Opaque
data:
  # echo -n "marquez" | base64
  marquez-db-password: bWFycXVleg==
