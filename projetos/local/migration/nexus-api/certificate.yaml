apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: nexus-api-tls
  namespace: nexus-api
spec:
  secretName: nexus-api-tls-secret
  issuerRef:
    name: letsencrypt-prod
    kind: ClusterIssuer
    group: cert-manager.io
  dnsNames:
  - migration-api.nexusplatform.com.br
