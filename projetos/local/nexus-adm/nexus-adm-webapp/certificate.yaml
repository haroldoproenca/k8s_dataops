apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: nexus-adm-webapp-tls
  namespace: nexus-adm-webapp
spec:
  secretName: console-tls-secret
  issuerRef:
    name: letsencrypt-prod
    kind: ClusterIssuer
    group: cert-manager.io
  dnsNames:
  - console.nexusplatform.com.br
