# Patch: nexus-adm-webapp Secrets (Opaque)
# Placeholder para variáveis sensíveis do frontend (se houver no futuro).
# SPAs geralmente não possuem segredos no lado do cliente.
apiVersion: v1
kind: Secret
metadata:
  name: nexus-adm-webapp-secrets
  namespace: nexus-adm-webapp
type: Opaque
data:
  # echo -n "placeholder" | base64
  UNUSED_SECRET: cGxhY2Vob2xkZXI=
