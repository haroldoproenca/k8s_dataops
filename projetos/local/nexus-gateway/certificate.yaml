apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: wildcard-nexus-cert
  namespace: nginx-gateway
spec:
  secretName: wildcard-tls-secret
  issuerRef:
    name: letsencrypt-prod
    kind: ClusterIssuer
  dnsNames:
  - "*.nexusplatform.com.br"
  - "nexusplatform.com.br"
