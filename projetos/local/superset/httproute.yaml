apiVersion: gateway.networking.k8s.io/v1
kind: HTTPRoute
metadata:
  name: superset-route
  namespace: superset
spec:
  parentRefs:
  - name: nexus-gateway
    namespace: nginx-gateway
    sectionName: tech-https
  hostnames:
  - "superset.tech.nexusplatform.com.br"
  rules:
  - matches:
    - path:
        type: PathPrefix
        value: /
    timeouts:
      request: 300s
      backendRequest: 300s
    backendRefs:
    - name: superset
      port: 8088
