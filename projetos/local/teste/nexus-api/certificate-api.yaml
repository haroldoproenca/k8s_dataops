apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: teste-api-tls-secret
  namespace: nexus-api
spec:
  secretName: teste-api-tls-secret
  issuerRef:
    name: letsencrypt-prod
    kind: ClusterIssuer
    group: cert-manager.io
  dnsNames:
  - teste-api.nexusplatform.com.br
