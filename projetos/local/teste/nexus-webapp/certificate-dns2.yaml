apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: teste-dns-tls-secret
  namespace: nexus-webapp
spec:
  secretName: teste-dns-tls-secret
  issuerRef:
    name: letsencrypt-prod
    kind: ClusterIssuer
    group: cert-manager.io
  dnsNames:
  - teste-dns2.nexusplatform.com.br
