apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: nexus-webapp-tls
  namespace: nexus-webapp
spec:
  secretName: teste-tls-secret
  issuerRef:
    name: letsencrypt-prod
    kind: ClusterIssuer
    group: cert-manager.io
  dnsNames:
  - teste.nexusplatform.com.br
