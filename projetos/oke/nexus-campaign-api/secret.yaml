apiVersion: v1
kind: Secret
metadata:
  name: nexus-campaign-api-secret
type: Opaque
data:
  # SUBSTITUIR pelos valores reais de produção antes de aplicar
  # echo -n "valor" | base64
  DATABASE_URL: REPLACE_WITH_BASE64_PROD_DATABASE_URL
  SECRET_KEY: REPLACE_WITH_BASE64_PROD_SECRET_KEY_64_CHARS
  REDIS_URL: REPLACE_WITH_BASE64_PROD_REDIS_URL
  NEXUS_CI_TOKEN: REPLACE_WITH_BASE64_NEXUS_CI_TOKEN
  NEXUS_CORE_WEBHOOK_SECRET: REPLACE_WITH_BASE64_WEBHOOK_SECRET
