apiVersion: gateway.networking.k8s.io/v1
kind: HTTPRoute
metadata:
  name: nexus-dashboard-webapp-route
  annotations:
    external-dns.alpha.kubernetes.io/hostname: dashboard.nexusplatform.com.br
spec:
  parentRefs:
  - name: nexus-gateway
    namespace: nginx-gateway
    sectionName: https
  hostnames:
  - "dashboard.nexusplatform.com.br"
  rules:
  - matches:
    - path:
        type: PathPrefix
        value: /
    backendRefs:
    - name: nexus-dashboard-webapp-service
      port: 80
