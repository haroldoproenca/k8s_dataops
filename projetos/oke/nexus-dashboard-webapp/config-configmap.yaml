apiVersion: v1
kind: ConfigMap
metadata:
  name: nexus-dashboard-webapp-config
data:
  config.js: |
    window.APP_CONFIG = {
      VITE_API_BASE_URL: "https://dashboard-api.nexusplatform.com.br",
    };
